export { generateMetadata, type SEOConfig } from "./metadata";
export { generateSchema, organizationSchema, websiteSchema, breadcrumbSchema, articleSchema, courseSchema, faqSchema, videoSchema, personSchema } from "./schema";
